#include <iostream>
#include <string>

using namespace std;
int main(int argc, char**argv)
{
	for(int i = 1; i < argc; i++)
	if(stoi(argv[i]) >= 1000)
	{
	
	{
		
		int x = stoi(argv[i]);
		cout << i-1;
		return 0;		
	}
}
	cout << -1;
	return 0;
	
}